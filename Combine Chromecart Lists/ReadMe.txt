This script combines lists of Chromebook serial numbers into a single list. This script was exported to an .exe for ease of use by others at the company.

The script requests a file path of where the lists of CHromebooks are stored, loops through each file, and appends each serial number to the end of the output file to place them itno a single list.

See .docx file in same directory for instructions of using this script.